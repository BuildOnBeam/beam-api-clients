namespace Beam.Test
{
    public class Tests
    {
        private const string ApiKey = "TODO";

        [SetUp]
        public void Setup()
        {
            var loggerMock = new 
            // var gameApi = new Beam.Api.GameApi(ApiKey);
        }

        [Test]
        public void Test1()
        {
            Assert.Pass();
        }
    }
}